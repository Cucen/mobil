var app = require('http').createServer(handler)      , io = require('socket.io').listen(app)      , fs = require('fs')      , mysql = require('mysql')    var client = mysql.createConnection({              host: 'localhost',              user: 'root',              password: '333444',    });    client.connect();    app.listen(3232);    function handler (req, res) {      fs.readFile(__dirname + '/testconnection.html',      function (err, data) {        if (err) {          res.writeHead(500);          return res.end('Error loading testconnection.html');        }        res.writeHead(200);        res.end(data);      });    }    io.sockets.on('connection', function (socket) {        socket.on('login', function(usr, pass){            var TEST_DATABASE = 'secim';            var TEST_TABLE = 'iller';            client.query('USE '+TEST_DATABASE);            client.query('SELECT * FROM iller', function(err, results) {              if (err) throw err;              console.log(results[0]); // [{1: 1}]              socket.emit('retuLogIn',results[0]['name']);            });        });        socket.on('disconnect', function(){            console.log('Server has disconnected');        });    });